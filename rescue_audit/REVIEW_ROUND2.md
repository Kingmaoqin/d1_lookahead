# 第二轮审查：对"不显著多半是你做错了"的正面回应

日期：2026-08-31（第一轮 rescue 结论发布之后）
触发：PI 指出并行的另一条实现线（ChatGPT）在做同一项目，要求派两个 review
agent 重查代码与实验，并明确提出"不显著的情况大概率是你做错了"。

---

## 0. 执行情况说明

派出了两个互不通气的审查 agent（Auditor A 查实现，Auditor B 查统计），
**两个都在启动后不久撞上会话速率上限而中断**（重置时间 21:10 America/Chicago）。
中断前的产出：

* **Auditor A**：已核实 `rdata.h_gm` 的留一实现**正确**、listwise 目标**没有
  退化**（我怀疑的两点被排除）；并写出了修正探针库
  `rescue_audit/rlib/probes3.py`（229 行，完整可用），其中提出了两条我自己
  **没有发现**的缺陷（下文 F1/F2）。
* **Auditor B**：只完成了仓库结构浏览，无实质产出。

其余工作由我接手完成。**Auditor A 提出的 F1/F2 都被证实为真，且严重。**

---

## 1. F1（CRITICAL，已证实）：Kronecker 探针复刻了原项目的 defect-4

`rescue_audit/rlib/probes2.py:fit_ridge_2block` 只支持**两个**块。
而我在 `screen.py` / `r2_stats.py` / `confirm_fresh.py` 里把
`[Zi(32) , Zg(32) , vec(Zi⊗Zg)(1024)]` 全部塞进**同一个隐藏块**，
共享一个 `gamma`、一个 `alpha`。

64 个主效应列与 1024 个交互列被同一强度收缩。这正是原项目自己识别为致命的
**缺陷 4**（"单一 α 无法同时正则化 100 维的控制块和 1536 维的隐藏块"）
在我自己代码里的复刻。

**实测证据**（`rescue_audit/results/` 各 JSON 的 `hp.gamma`）：

| 探针 | 选中的 gamma | 含义 |
|---|---|---|
| `additive_pca`（无交互项） | **3.0**（6 处中 5 处） | 撞网格上界，想要更大权重 |
| `kron_pca`（含 1024 交互列） | **0.003 – 0.03** | 被迫重度收缩 |

两个模型**根本不在同一个惩罚点上**。因此原先报告的
"kron < additive ⇒ 交互项无用" **不成立**——那个差可能全部来自惩罚错配。

## 2. F2（MAJOR，已证实）：gamma 网格撞上界

`probes2.fit_ridge_2block` 默认 `gammas` 上界 3.0，而 `additive_pca` 在
6 处出现中有 **5 处选中 3.0**。隐藏块的权重被网格卡住。

## 3. 修正与重测

`rlib/probes3.fit_ridge_blocks`（Auditor A 写，我审过并使用）：

* **每块独立 gamma** → 每块独立惩罚 `alpha/gamma²`；
* gamma 上界从 3.0 提到 100，我进一步提到 **10000** 以确认饱和；
* **加性与 kron 从同一次搜索里取嵌套切片**（加性 = 交互块 gamma=0），
  于是 kron 在验证集上按构造 ≥ 加性，test 上的任何劣势只能来自过拟合。

### 3.1 先在已知答案的合成数据上验证修复有效

| 场景 | cheap | 加性 | kron | **关系型 Δconc** | 选中的 γ_int |
|---|---|---|---|---|---|
| **B 双线性（植入信号）** | 0.4993 | 0.4993 | **0.5740** | **+0.0747** | **10.0** |
| F 零假设 | 0.4909 | 0.4909 | 0.5000 | +0.0091 | 1.0 |
| A 线性 | 0.6298 | 0.6960 | 0.6993 | +0.0033 | 0.01 |

两点：
1. **修复确实有效**：修正后的搜索在合成 B 上把关系型效应从旧实现的 ≈0 提到
   **+0.0747**，并且选了 **γ_int = 10.0 —— 旧网格上界 3.0 根本够不到**。
2. **同时给出了噪声底线**：合成 F 上关系型 Δconc = **+0.0091**。放大后的搜索
   （约 3700 组配置在验证集上选）带来约 +0.009 的选择乐观偏差。
   **真实数据上低于 +0.01 的关系型效应无法与选择噪声区分。**

### 3.2 真实数据（修正实现，网格已解开）

| 数据 | 层 | 池化 | 加性 Δconc | **关系型 Δconc** | 选中的 γ_int |
|---|---|---|---|---|---|
| MDLM anc | 6 | hg | +0.0127 | **+0.0002** | 0.01 |
| MDLM anc | 8 | hg | +0.0120 | +0.0001 | 0.01 |
| MDLM anc | 9 | hg | +0.0117 | +0.0000 | 0.01 |
| MDLM anc | 6 | **hgm** | +0.0112 | −0.0011 | 0.01 |
| MDLM anc | 8 | **hgm** | +0.0122 | +0.0001 | 0.01 |
| MDLM anc | 9 | **hgm** | +0.0101 | −0.0008 | 0.03 |
| MDLM conf | 6 | hg | +0.0089 | −0.0027 | 0.03 |
| MDLM conf | 8 | hg | +0.0136 | −0.0012 | 0.03 |
| SEDD | 6 | hg | +0.0041 | **+0.0000** | **0.0（直接拒绝交互块）** |
| SEDD | 8 | hg | +0.0019 | −0.0007 | 0.01 |
| FRESH（post-hoc） | 6 | hg | +0.0093 | −0.0031 | 0.01 |
| FRESH（post-hoc） | 8 | hg | +0.0133 | −0.0004 | 0.01 |

**全部 12 个配置的关系型 Δconc 落在 ±0.003 内**，对照：同一套搜索在
合成 B 上是 +0.0747，在合成 F 的选择噪声底线是 +0.009。

> 最有说服力的一句：**同一套修正后的搜索，在植入了双线性信号的数据上会去选
> γ_int = 10.0，在真实数据上一律只选 0.01（等于不用）。模型类相同、网格相同、
> 选择准则相同，差别只在数据本身。**

### 3.3 F2 的代价与 S3 的了结

* 解开 gamma 上界后，加性 Δconc 从 **+0.0107 → +0.0127**（MDLM anc L6）。
  **F2 确实压低了效应，但只值 +0.002**；继续把上界提到 10000，选中值移到
  300/1000 而 concordance **一模一样**（0.7912），说明已经饱和。
* `hgm`（S3 提出的掩码位留一池化）与 `hg` 结果**无差别**。
  报告里"S3 限制了关系型探针表达力"这条担心，实测**不成立**。

---

## 4. 我自己新做的检查：标签的排序可靠性天花板

这是 PI 质疑的另一种可能解释——**如果 cheap 已经贴着标签噪声天花板，
那"没有探针能超过 cheap"就是测量精度问题，与表征无关**。
`rescue_audit/label_reliability_ceiling.py`。

做法：把 K=24 条 rollout 种子随机分成两个不相交的 m 条子集，各自求均值
（信号相同、噪声独立），算 `concordance(Ā_B, Ā_A)`。

| 每半种子数 m | 2 | 3 | 4 | 6 | 8 | 12 |
|---|---|---|---|---|---|---|
| 实测 concordance | 0.6377 | 0.6675 | 0.6913 | 0.7272 | 0.7514 | 0.7844 |
| 可靠性模型预测 | 0.6260 | 0.6611 | 0.6878 | 0.7265 | 0.7536 | 0.7900 |

模型（`r(m)=σ²_s/(σ²_s+σ²_n/m)`，`conc=0.5+arcsin(ρ)/π`）在三个臂 ×
六个 m 上最大偏差 **0.014**，因此可以用它外推完美预测器的上限。

**【一处必须写明的自我修正】** 这个分析的第一版对 `conc(1/m)` 做线性外推，
得到 0.79 并准备把它当作"concordance 上限"。那是**错的**：
(a) 该曲线关于 1/m 是凸的；(b) 更根本地，它衡量的是"一个**同样带噪**的测量
去排序"，而 m→∞ 时两半都无噪、真极限是 1.0。正确的量是
**完美预测器与实际 K=24 标签的 concordance**，即 ρ=√r(K)。

| 臂 | cheap | cheap+H | **完美预测器上限** | cheap 之上剩余 | 隐藏态吃掉 |
|---|---|---|---|---|---|
| MDLM anc | 0.7786 | 0.7892 | **0.8887** | **+0.1102** | 9.7% |
| SEDD | 0.8086 | 0.8112 | **0.8988** | +0.0902 | 3.0% |
| FRESH | 0.7473 | 0.7536 | **0.8863** | +0.1390 | 4.5% |

> **"天花板太低"这个解释被排除。** cheap 之上还有 **0.09–0.14** 的 concordance
> 空间，而冻结表征只吃掉其中 **3–10%**。
>
> 这让否定结论比第一轮**更锋利**，同时也更有内容：
> 标签里**确实存在**大量候选级排序信号（占可达空间的 90%+），
> 只是既不在廉价输出特征里、也不在冻结表征里。
> 原来的说法"候选级信息不存在"是不准确的；准确的说法是
> **"候选级信息存在于标签中，但冻结表征不携带它"**。

---

## 4b. 新发现（post-hoc）：隐藏态的增益集中在最难的候选子集上

任务书 §15 要求把 **natural** 与 **informative** 两个候选层分开报告，
但全项目从未对 **concordance** 这么做过（只对 R² 做过）。
`rescue_audit/stratified_concordance.py` 把每个 state 的 15 个配对拆成三类：
3 个 natural 内部配对、3 个 informative 内部配对、9 个跨层配对。

第 8 层，Δconcordance（cheap+H − cheap），文档聚类自助 1000 次：

| 数据 | 子集 | cheap | cheap+H | **Δhidden** | 95% CI | 排除 0 |
|---|---|---|---|---|---|---|
| MDLM anc | within-natural | 0.7783 | 0.7833 | +0.0053 | [−0.0159,+0.0264] | ✗ |
| MDLM anc | **within-informative** | **0.7506** | 0.7761 | **+0.0258** | **[+0.0038,+0.0506]** | **✓** |
| MDLM anc | cross | 0.7880 | 0.7950 | +0.0069 | [−0.0049,+0.0186] | ✗ |
| MDLM anc | 全部（池化） | 0.7786 | 0.7889 | +0.0103 | [+0.0009,+0.0198] | ✓ |
| FRESH | within-informative | 0.7078 | 0.7278 | +0.0198 | [−0.0073,+0.0460] | ✗ |
| SEDD | within-informative | 0.7783 | 0.7861 | +0.0075 | [−0.0116,+0.0272] | ✗ |

读法：

* **informative 子集正是廉价特征最弱的地方**（cheap 0.7506 vs natural 0.7783），
  而隐藏态在那里贡献 **+0.0258 —— 是池化值的 2.5 倍，且超过预注册阈值 0.020，
  CI 排除 0**。三个数据集上 within-informative 都高于另外两个子集，方向一致。
* 但这是一个**典型的需要专门确认性检验的 post-hoc 亚组发现**：
  在发现它的那份数据上显著，在 fresh holdout 上**方向一致但 CI 含 0**
  （只有 50 个 test 文档 / 900 个配对，严重欠功效），在第二骨干上很弱。
* 另外 informative 层是**人为过采样**的子总体（每 state 3 个），
  自然分布下的数字是 within-natural + cross 那部分，
  它们的 Δ 分别是 +0.0053 与 +0.0069，CI 都含 0。

**它不推翻判定**，但它是 Path-LL 这条线上最强的线索，且完全在任务书 §15
事先要求的分析范围内。下一轮如果还做 Path-LL，应当预注册
"informative 子集上的 Δconcordance"作为主端点，并把每 state 的
informative 候选数从 3 提到 8–12 以拿到功效。

顺带排除一个混淆：跨层配对**不是**靠置信度就能白拿的——
仅用置信度排序在 cross 上是 0.7835，cheap 全特征 0.7880，
只比 within-natural（0.7667 / 0.7783）高一点点。
分层结构没有明显虚高 cheap 基线。

## 5. 层锁的代价（post-hoc）

预注册把层锁死在 6（来自 {3,6,9,11} 的粗扫）。但广度扫描
（`R1_MDLM_anc_broad.json`）显示加性探针在三个划分种子上验证集选中的层
一致是 **8 或 9**。

在 fresh holdout 上 post-hoc 复算：加性 Δconc 在 L6 = +0.0093，
**L8 = +0.0133**。**层锁付了约 +0.004 的代价**，但即便按 L8 也远低于
阈值 0.020。完整的 post-hoc 统计见 `rescue_audit/results/POSTHOC_fresh_L8.json`
（**必须标注为 post-hoc，不能当确认性证据**）。

---

## 5b. F3（torch 训练预算）—— 已重测，结论不变

**F3 属实**：在 103 处保存的最优超参里，`lr` 取网格最大值 3e-3 的比例是
**74%**（`wd` 不撞边界，45/55 分布）。因此原先所有非凸探针的负结果
在重测前**不能**排除欠训。

用 `probes3.Runner3` 重测：**3000 步（原 400 步，7.5×）**，
lr 上探到 3e-2，wd 含 0.0，patience 500，`calib="within"`。

先在已知答案的数据上确认加大预算之后探针仍然工作、且不假阳性：

| 场景 | `rank_cand`（仅候选） | `rank_cand_state`（**加状态条件**） | 差 |
|---|---|---|---|
| 合成 B（植入双线性） | +0.0096 | **+0.1378** | **14×** |
| 合成 F（零假设） | −0.0012 | −0.0011 | 0 |

（合成 B 上 `bilinear_r4` within-R² +0.5228、`film` +0.1811；
合成 F 上全部 ≤ +0.0007，concordance 0.489–0.509，**零假阳性**。）

真实数据（MDLM anc，第 8 层，test）的**同族对照**——每一对都只差
"有没有关系型/状态条件成分"：

| 关系型版本 | conc | 非关系型对照 | conc | **Δ** |
|---|---|---|---|---|
| `relmlp_w512` | 0.7603 | `cheaponly_mlp_w512` | 0.7766 | **−0.0162** |
| `relmlp_w128` | 0.7587 | `cheaponly_mlp_w128` | 0.7788 | **−0.0201** |
| `rank_cand_state` | 0.6457 | `rank_cand` | 0.6528 | **−0.0071** |
| `rank_cheap_hidden` | 0.7810 | `rank_cheap` | 0.7886 | **−0.0076** |

**四对全部为负。** 而且真实数据上最好的探针是
`rank_cheap`（Δconc +0.0100、top1 0.458）——**廉价特征 + 排序损失，
完全不用隐藏态**。

**F3 至此了结：非凸探针的负结果不是欠训造成的。**
另有一个诊断细节：加大预算后最优 `best_epoch` 落在 **10–110**，
说明这些模型本来就很早收敛；原先 lr 撞上界是"想更快收敛"的症状，
不是训练不足。

## 5c. 补上最后一个空白：P11 时序差分 / 合成 C

这是任务书探针族里**唯一从未被检验过**的假设——候选级未来价值是否编码在
`Δh_i = h_{i,t} − h_{i,t−1}` 而非静态 `h_{i,t}` 里。以前做不了是因为分片
没存上一步的隐藏态。

**补采**（`rescue_audit/collect_prev_hidden.py`）：π_ref 的轨迹在给定种子下
完全确定（order key 与 token Gumbel 都由 (seed, 位置, token id) 散列，不含
状态），因此可以原样重走、只做前向、把上一步的隐藏态截下来，成本约为原采集
的 1/50（400 prompts 约 12 分钟）。

**自校验**：重走时同样截下当前步的 h_i / h_g 与盘上已存的比对——
a3 与 b3 各 7200/7200 行，**max|ΔH_i| = 0、max|ΔH_g| = 0**，逐比特复现。
这同时验证了"轨迹确定性"这个前提本身。

**结果**（三块岭回归 cheap | static | temporal，temporal 块 gamma 网格含 0，
故 static 是嵌套子模型；全量 14,400 行）：

| 层 | 合成 C：静态 within-R² | 合成 C：**时序** within-R² | 合成 C Δconc | **真实数据 时序 Δconc** | 真实选中的 γ_temporal |
|---|---|---|---|---|---|
| 6 | +0.0123（失败） | **+0.5896** | +0.0950 | **+0.0000** | **0.0** |
| 8 | +0.0082（失败） | **+0.5703** | +0.1160 | **+0.0000** | **0.0** |
| 9 | +0.0035（失败） | **+0.5522** | +0.1291 | **+0.0000** | **0.0** |

合成 C 按要求的模式通过：**静态探针失败、时序探针成功**。
真实数据上则是本轮最干净的一个否定——**验证集选择把时序块的权重设成
精确的 0，直接拒绝这个特征块**，而同一套搜索在植入时序信号的数据上会启用它
并拿到 +0.10~+0.13。

顺带：static 块在 L8 上的 Δconc = **+0.0142**，是原始数据上测到的最大
隐藏态效应（PCA 维度 64）。仍 < 0.020。

## 6. 结论：判定是否改变？

**不改变，但两处必须修订措辞。**

| 项 | 第一轮说法 | 修订后 |
|---|---|---|
| 关系型假设 | "kron 显著为负（−0.0073），交互项有害" | **该数字是惩罚错配的产物，作废。** 修正实现下关系型 Δconc = **±0.003，即精确为零**；且探针在植入信号时能拿到 +0.075。结论方向不变但证据更强、措辞需改。 |
| 候选级信息 | "表征不携带候选级信息" | **"标签里存在大量候选级信号（cheap 之上还有 0.09–0.14 concordance），冻结表征只贡献其中 3–10%。"** |
| 加性效应量 | Δconc +0.0107 | **+0.0127**（解开 gamma 上界后），仍 < 0.020 |
| 层选择 | 锁 L6 | L8 更优，post-hoc +0.0133，仍 < 0.020 |
| 非凸探针 | 400 步，可能欠训 | **3000 步重测，四对同族对照全为负**（§5b） |
| 候选分层 | 未拆 | **informative 子集 Δconc +0.0258，CI 排除 0，超阈值**（post-hoc，§4b） |

**KILL CONFIRMED 的判定成立**，但它现在建立在一个更强的基础上：
不是"我们找不到"，而是"在一个被证明能找到该类结构、且惩罚配置正确、
网格不撞边界、标签天花板留有 0.11 空间的搜索下，仍然找不到"。

## 7. 仍然没做的（诚实清单）

1. **两个 review agent 都没跑完。** 上面的复查是我自己做的，
   **不构成独立审查**。速率限制解除后应重跑，尤其是 Auditor B 那条
   （统计与设计），它一行结论都没产出。
2. ~~F3 未重测~~ **已完成，见 §5b**：3000 步 + 更宽网格下四对同族对照全部
   为负，合成对照上同一批探针有 14× 的正响应。
3. ~~合成 C（时序 delta）仍未做~~ **已完成，见 §5c**：补采逐比特精确，合成 C 通过，真实数据上时序块被选择过程直接置零。
4. `probes2` 里的旧结果（`R1*`、`R2stats_*`、`CONFIRMATORY_freshA.json`）
   保留未删，但其中所有 `kron vs additive` 的数字都受 F1 影响，
   引用时必须以本文件 §3.2 为准。
